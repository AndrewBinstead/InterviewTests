describe('Location controllers', function() {
  beforeEach(module('weatherApp'));
 
  describe('Location controller', function(){
 
//    it('should provide 4 locations', inject(function($controller) {
//      var scope = {};
//      var ctrl = $controller('locationCtrl', { $scope: scope });
 
      //expect(scope.locations.length).toBe(4);
//    }));
  });
});